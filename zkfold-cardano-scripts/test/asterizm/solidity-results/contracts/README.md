# Solidity code

Solidity contract `AsterizmHashLib.sol` was executed in [REMIX](https://remix.ethereum.org) using helper contract `HashHarness.sol`.

Resulting hashes are collected in directory `hash-results`.